# AWS Lambda Deployment Dependencies
#
# boto3/botocore are provided by the AWS Lambda Python runtime — do not bundle.
# strands-sdk is only used by dev/test tooling (check_env.py, test_*.py),
# not by runtime code — do not bundle.

requests>=2.34.2
